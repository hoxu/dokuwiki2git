data/attic/<pagepath>.<timestamp>.txt.gz

data/media ?

data/meta/<pagepath>.changes
	tab-delimited
	<timestamp> <ip> [CEe] <pagepath> <author> <comment>

	changes = (timestamp, ip, type, pagepath, author, comment)
		# sort by timestamp after all changes have been collected

